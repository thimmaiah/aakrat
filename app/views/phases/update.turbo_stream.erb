
    <%= turbo_stream.replace("#{dom_id @phase}",
        partial: "phases/phase", locals: { phase: @phase }) %>

    <%= turbo_stream.replace('toasts', partial: "layouts/success_toast",
        locals: { message: "Phase '#{@phase.name}' updated" }) %>

    <% 
        phase = @phase.dup 
        phase.name = nil
    %>

    <%= turbo_stream.replace('phase_quick_entry', partial: "phases/quick_entry",
        locals: { phase: phase }) %>