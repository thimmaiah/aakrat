
    <%= turbo_stream.prepend('phases_tbody',
        partial: "phases/phase", locals: { phase: @phase }) %>

    <%= turbo_stream.replace('toasts', partial: "layouts/success_toast",
        locals: { message: "Phase '#{@phase.name}' created successfully" }) %>

